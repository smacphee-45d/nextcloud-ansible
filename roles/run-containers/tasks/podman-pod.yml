- name: create nextcloud pod 
  containers.podman.podman_pod: 
    name: nextcloud_pod
    state: started
    ports:
     - ' {{ http_port }} '
     - ' {{ https_port }} '
    generate_systemd:
      path: /etc/systemd/system/
      restart_policy: always
      time: 120
      names: true